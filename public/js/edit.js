const editarVentana = document.getElementById("ventana-editar");

const abrirEditar = () => {
  editarVentana.style.display = "flex";
};

const cerrarEditar = () => {
  editarVentana.style.display = "none";
};
